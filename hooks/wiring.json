{
  "version": 1,
  "_b17": "GRVHK",
  "_provisional": "HAND-AUTHORED, AND THAT IS THE 1.4 MISTAKE THIS PROPOSAL EXISTS TO AVOID. governance/proposals/2026-09-02-grove-hooks-and-skills.md §6 requires this file to be GENERATED from sealed Nestor rows by a script under scripts/, in the same PR that seals them, with a test comparing the manifest on disk to the rows in the store. None of that exists yet. This file was written by hand for one purpose: to find out whether willow-mcp's hook_manifest path works at all before any row is written against it, because no project in the 27-entry registry had ever declared one. Delete this key when the derivation script lands; until then, do not add rows here.",
  "command": "hooks/grove-hook",
  "env": {
    "GROVE_SEAT_FILE": "{{PROJECT_ROOT}}/hooks/seat.md"
  },
  "hooks": [
    {
      "event": "session_start",
      "action": "orient",
      "_answers": "§2 questions 1 and 2 at boot — seat, seam reach, packet, envelope, attestation, corpus age, unit manifest, port ownership"
    },
    {
      "event": "prompt_submit",
      "action": "reinject",
      "_answers": "§3 — keeps question 1 alive under compaction. Three lines, byte-stable. This is also the only mid-session surface: a lease or attestation that expires at hour three is caught here, not at session start."
    },
    {
      "event": "pre_compact",
      "action": "reinject",
      "_answers": "§3 — same three lines; compaction is where the seat rule is most likely to be lost"
    },
    {
      "event": "stop",
      "action": "gate",
      "_answers": "§3 question 3, per turn — refuse a turn claiming done with no evidence token; block once, then advisory"
    },
    {
      "event": "session_end",
      "action": "deposit",
      "_answers": "§3 question 3, per session — journal the decisions, propose the pairs, write the stack snapshot. Cannot block (Nestor's finding); warns and flushes."
    }
  ],
  "_no_pre_tool_use": "Deliberate, per §7: willow-mcp's PreToolUse remains the one gate on tool calls. Grove adds no second gate on shell, write or MCP. One guard per event. Grove's stop gate gates TURNS, which is a different event."
}
