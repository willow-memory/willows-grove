# Generated ADR stubs

**b17:** ADRST · ΔΣ=42  

Files here are **machine-written drafts** from `scripts/grove_docs_extract.py --stubs`. They are **not** ratified ADRs until a human completes sections and moves them to `docs/adrs/ADR-*.md` (see [`../README.md`](../README.md)).

Regenerate:

```bash
python3 scripts/grove_docs_extract.py --limit 10000 --stubs --stub-limit 10 --out docs/generated all
```
